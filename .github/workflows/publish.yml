name: Publish

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  attestations: write
  contents: write
  id-token: write

jobs:
  nif-release:
    uses: leandrocp/github-actions/.github/workflows/nif-release.yml@main
    with:
      project-name: mdex_native_nif
      project-dir: native/mdex_native_nif

  hex-publish:
    needs: nif-release
    if: startsWith(github.ref, 'refs/tags/v')
    uses: leandrocp/github-actions/.github/workflows/hex-publish.yml@main
    with:
      env-vars: '{"MDEX_NATIVE_BUILD":"1"}'
      install-rust: true
      pre-publish-command: mix rustler_precompiled.download MDExNative.Native --all --print
    secrets:
      HEX_API_KEY: ${{ secrets.HEX_API_KEY }}
